{
  "name": "slack-to-github-image-uploader",
  "version": "1.0.0",
  "description": "Cloudflare Worker for uploading Slack images to GitHub with metadata",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "test": "vitest",
    "format": "prettier --write \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare-workers",
    "slack",
    "github",
    "image-processing"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "@types/node": "^20.11.5",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3",
    "vitest": "^1.2.1",
    "wrangler": "^3.25.0"
  },
  "dependencies": {
    "browser-image-compression": "^2.0.2"
  }
}
